# Server
PORT=3000
NODE_ENV=development

# Optional email config for /api/contact-email
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=username
SMTP_PASS=password
MAIL_FROM="Osho Digital Library <no-reply@example.com>"
MAIL_TO=owner@example.com
